#!/bin/bash -e

source LifoConfig
Include 'recipelib/functions'

SourceRecipe $1

declare -p Title
declare -p Name
declare -p Version
declare -p Supplies

for supply in "${Supplies[@]}"; do
    echo "supply: $supply"
done
