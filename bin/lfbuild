#!/bin/bash

source $(dirname $0)/tools/functions.sh

if [ "$#" == "1" ]; then
    lfCmd=all
    lfScript=$(get-lfs-script $1)
fi
echo "lfCmd=${lfCmd}"
echo "lfScript=${lfScript}"

echo $(get-lfs-script $1)